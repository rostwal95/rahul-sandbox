# Web (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_ASSET_BASE=http://localhost:9000/dev-bucket

# API (Rails)
RAILS_ENV=development
DATABASE_URL=postgres://postgres:postgres@postgres:5432/kit_builders_dev
REDIS_URL=redis://redis:6379/0
SECRET_KEY_BASE=devsecretchangeme
RUM_SAMPLE_RATE=0.2

# Mail / Webhooks
SMTP_ADDRESS=mailhog
SMTP_PORT=1025
SENDGRID_PUBLIC_KEY=<REPLACE_WITH_BASE64_PUBLIC_KEY>

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_DEFAULT_PRICE_ID=price_123
BILLING_SUCCESS_URL=http://localhost:3000/billing?success=true
BILLING_CANCEL_URL=http://localhost:3000/billing?canceled=true

# Use local stub logic instead of real Stripe calls
FAKE_STRIPE=true

# AWS / Uploads
AWS_REGION=us-east-1

# For offline local S3 emulator (MinIO). Set S3_EMULATOR=true to bypass presign.
S3_EMULATOR=true
S3_BUCKET=dev-bucket
ASSET_PUBLIC_BASE=http://localhost:9000/dev-bucket
AWS_ACCESS_KEY_ID=minio
AWS_SECRET_ACCESS_KEY=minio12345

# SendGrid (for SMTP or webhook verification)
MAIL_PROVIDER=sendgrid
SENDGRID_API_KEY=SG.XXXX
SENDGRID_PUBLIC_KEY=BASE64_ED25519_PUBKEY

# SES (alternative SMTP)
SES_SMTP_ADDRESS=email-smtp.us-east-1.amazonaws.com
SES_SMTP_USERNAME=AKIA...
SES_SMTP_PASSWORD=...

# Billing portal
BILLING_PORTAL_RETURN_URL=http://localhost:3000/billing

OPENAI_API_KEY=

# Next Image optimization remote
NEXT_PUBLIC_S3_BUCKET_HOST=your-bucket.s3.amazonaws.com
