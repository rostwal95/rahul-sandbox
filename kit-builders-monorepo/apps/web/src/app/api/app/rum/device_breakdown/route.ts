import { forward } from '@/lib/proxy'; export async function GET(req:Request){ const u=new URL(req.url); const slug=u.searchParams.get('slug'); const from=u.searchParams.get('from'); const to=u.searchParams.get('to'); const qs = new URLSearchParams({ ...(slug?{slug}:{}) as any, ...(from?{from}:{}) as any, ...(to?{to}:{}) as any }); return forward(`/v1/rum/device_breakdown${qs.toString()?`?${qs.toString()}`:''}`); }
