import { forward } from '@/lib/proxy'; export async function GET(req:Request){ const u=new URL(req.url); const id=u.searchParams.get('broadcast_id'); return forward(`/v1/exports/broadcast_clicks?broadcast_id=${id}`); }
